# บันทึกการ recheck Skill 3 · 2026-08-21

ตรวจว่า **Skill 3 (ตามอ่านกฎหมายที่ตัวบทอ้างถึง)** ยังไปหาข้อมูลมาได้จริงหรือไม่
โดยวัด 5 ข้อ: ดึงได้จริง · เอาเฉพาะที่อ้างถึงโดยตรง · ถูกต้อง · ครบ · ไม่ช้า

ผลสรุป: **กลไกใช้งานได้ · ด่านตรวจความถูกต้องแน่นดี · จุดที่ต้องแก้อยู่ที่ "ความครบ"**
แก้ไปทั้งหมด 6 จุด ทุกจุดมีหลักฐานจากการยิงทดสอบจริง ไม่ได้แก้จากการอ่านโค้ดอย่างเดียว

---

## ขอบเขตของการตรวจ

| ตรวจได้ | ตรวจไม่ได้ |
|---|---|
| ชั้นดึงไฟล์ตัวบท (ยิง HTTP จริงทุกโดเมน) | เส้น end-to-end ที่ต้องเรียก Anthropic API |
| ตรรกะแจกโควตา (จำลองครบทุกเคส) | คุณภาพคำตอบที่โมเดลคืนมาจริง |
| นิยามเครื่องมือ · ด่าน `hostAllowed` (โหลดโมดูลจริง) | พฤติกรรมบน Vercel จริง |
| syntax ทุกไฟล์ · `npm run build` | |

**เหตุผล:** ไม่มี `.env` / `ANTHROPIC_API_KEY` ในเครื่อง
สิ่งที่ทดสอบสดได้คือชั้นดึงไฟล์ ซึ่งเป็นจุดที่พังบ่อยที่สุดอยู่แล้ว

---

## ผลวัดจริง — แหล่งตัวบทหลักยังใช้ได้

ราชกิจจานุเบกษา รูปแบบ `/documents/<id>.pdf` — **3/3 ผ่าน**

```
200  application/pdf   84,583 B   1.9s   ratchakitcha.soc.go.th/documents/104277.pdf
200  application/pdf   63,534 B   2.6s   .../17097.pdf
200  application/pdf   26,300 B   2.6s   .../140021.pdf
```

และของที่โค้ดบันทึกไว้ว่าตายแล้ว **ตายจริงตามนั้นทุกตัว**

| ที่อยู่ | ผล |
|---|---|
| `/documents/<id>` (ไม่มี `.pdf`) | 403 |
| `/DATA/PDF/...` | 403 (Cloudflare) |
| `legal.labour.go.th` | ต่อไม่ติด |
| `krisdika.go.th` | `ENOTFOUND` |
| `www.krisdika.go.th` | `DEPTH_ZERO_SELF_SIGNED_CERT` |

`DEAD_SOURCES` และการถอด krisdika ออกจาก Skill 3 **ถูกต้องตามสภาพจริง**

---

## สิ่งที่แก้

### 1 · `specific` ถูกเบียดตกทั้งหมด — `osh-law-relate.js`

`MAX_ANCHOR_LOOKUPS` = `MAX_TOTAL_LOOKUPS` = 12 · `anchor` หยิบก่อนและหยิบได้ครบ 12
ตัวบทที่อ้างลอย ๆ 12 จุด (ไม่แปลกในกฎกระทรวงฉบับเดียว) กินโควตาหมดตั้งแต่คลื่นแรก
เหลือ 0 ให้ `pending recheck` และเหลือ 0 ให้ลูป `specific` ซึ่งมีเงื่อนไข `budget > 0`

> **การอ้างที่ระบุเลขมาตราชัดเจน — ตรงที่สุดและถูกที่สุด — ถูกการอ้างลอย ๆ เบียดตกทั้งหมด**

**แก้:** เพิ่ม `MIN_SPECIFIC_RESERVE = 6` กันโควตาไว้ก่อนแจก **เท่าที่มีของจริง**
ไม่มี `specific` สักตัว = ไม่กัน · `anchor` ยังได้เต็ม 12 เหมือนเดิม

จำลองครบทุกเคส (a≤14 × p≤8 × s≤14 = 2,025 เคส):

```
เคสที่ยอดรวมงานที่ทำได้ลดลงจากของเดิม : 0
เคสที่ทะลุเพดาน 12                    : 0

a=12 p=0 s=6 | เดิม {anchor:12, pending:0, specific:0}
             | ใหม่ {anchor:6,  pending:0, specific:6}
```

### 2 · โน้ตบอกสาเหตุผิด — `osh-law-relate.js`

`related_laws` เขียนโน้ตว่า *"เวลาในรอบนี้ไม่พอ"* ตายตัวให้ `frontier` ที่ค้าง
ทั้งที่ลูปหยุดเพราะ **โควตาหมด** ก็เข้าทางนี้ได้ · สองอย่างนี้ผู้ใช้ต้องทำคนละอย่าง

- เวลาไม่พอ → กดสรุปซ้ำแล้วดีขึ้นจริง (cache ช่วย)
- โควตาหมด → **กดซ้ำก็ได้ผลเดิมทุกครั้ง** ต้องเปิดตัวบทเอง

**แก้:** เพิ่ม `frontierWhy` (`'time'` / `'budget'`) เขียนโน้ตตามเหตุจริง
และ `skipped_for_time` นับเฉพาะที่ตกเพราะเวลาเท่านั้น

### 3 · `whole_law` ที่ไม่ได้โควตาหายเงียบ — `osh-law-relate.js`

`answers` มีแต่ `anchorTake` · ฝั่ง `pending` ที่เกินยังได้ `pendingAnswer()`
แต่ `anchor` ที่เกิน **ไม่ได้อะไรเลย ไม่โผล่ที่ไหนสักที่**
ผู้ใช้จึงไม่มีทางรู้ว่ามีจุดที่ระบบไม่ได้ตามไปอ่านให้

**แก้:** `anchorLeftOver` ออกมาเป็นแถว `status: 'manual'` ใน `related_laws`
พร้อม `anchor_question` และ `for_section` เหมือน `deferred` ของฝั่ง `specific`

### 4 · โดเมนตาย — `law-source.js`

**ถอด `dlpw.go.th`** — `ENOTFOUND` ทั้ง apex และ `www.` โดเมนทั้งโดเมนไม่ resolve
อาการเดียวกับ krisdika ที่โค้ดสั่งห้ามใส่กลับ · ตัวบทกรมสวัสดิการฯ ยังเปิดได้ที่
`osh.labour.go.th` ซึ่งอยู่ใต้ `labour.go.th` ในรายการแล้ว
แก้ `SOURCE_URL_RULES` ที่เคยบอกโมเดลให้ใช้โดเมนนี้ด้วย

**คง `doeb.go.th` ไว้** ทั้งที่ `fetch()` ของเราใช้ไม่ได้
(`Response does not match the HTTP/1.1 protocol` · Imperva · ล้มเร็ว ~1.3 วิ ไม่ค้าง)
เหตุผลที่ไม่ใส่ `DEAD_SOURCES`: `web_search` รันบนเครื่องของ Anthropic ไม่ใช่เครื่องเรา
ยังอ่านหน้านี้และคืน excerpt ที่ผ่านด่านหลักฐานได้จริง (เส้นทาง `firstRoundComplete`)
ใส่เมื่อไร = ตัดคำตอบที่ใช้ได้ทิ้งไปด้วย

### 5 · `PDF_FETCH_TIMEOUT` 45 → 20 วิ — `law-source.js`

⚠️ **ข้อนี้แก้จากข้อสรุปที่ผิดของรอบแรก** — รอบแรกสรุปว่า `osh.labour.go.th` กินงบ 45 วิ
วัดแยก header/body แล้วพบว่า **ไม่จริง**: header กลับมาใน 4.6 วิ พร้อม `content-type: text/html`
ซึ่ง `fetchPdfBase64` ตัดทิ้งตรงด่าน content-type ทันที ไม่ค้าง · ที่ค้างคือ probe เองที่ไปอ่าน body ต่อ

ที่กินงบได้จริงคือโฮสต์ที่ตอบ `application/pdf` แล้ว **body ค้างกลางทาง** = 18% ของงบ 255 วิ ต่อ 1 ที่อยู่
เป็นความเสี่ยงหางแถวที่ยังไม่เคยสังเกตพบ ไม่ใช่รูรั่วที่วัดได้
ไฟล์ที่ดึงได้จริงใช้ 1.9 / 2.6 / 2.6 วิ · เว็บที่ยังตอบอยู่ตอบภายใน ~13 วิทุกตัว → 20 วิเผื่อเกินพอ

### 6 · อัปเวอร์ชันเครื่องมือค้น — `law-source.js` + `osh-law-relate.js`

`web_search_20250305` → **`web_search_20260209`** (dynamic filtering — รันโค้ดกรองผลค้นให้ในตัว
ก่อนส่งให้โมเดลอ่าน ตรงกับงานนี้เพราะเว็บราชการคืนหน้าสารบัญ/หน้ารวมลิงก์ปนมาเยอะ)

**แก้ต้นเหตุด้วย ไม่ใช่แค่เลขเวอร์ชัน:** `osh-law-relate.js` ก๊อปนิยามเครื่องมือมาเขียนซ้ำเอง
ทั้งที่ `import { WEB_SEARCH_TOOL }` มาแล้วแต่ไม่ได้ใช้
อัปแค่ตัวกลางแล้วฝั่ง `specific` จะยังยิงเวอร์ชันเก่าอยู่เงียบ ๆ · ตอนนี้ชี้มาที่ตัวกลางแล้ว
นี่คือ drift แบบเดียวกับที่หัวไฟล์ `law-source.js` เตือนไว้เอง — และเกิดขึ้นจริงตอนอัปครั้งนี้

---

## เจอเพิ่มระหว่างทาง (นอกขอบเขต Skill 3)

`api/law-analyze.js` มี allowlist **คนละชุด** กับ Skill 3 (`ALLOWED_HOSTS`)
และ **fetch เองจากเซิร์ฟเวอร์เรา** (`:377`) ในนั้นมีทั้ง `dlpw.go.th` และ `krisdika.go.th`
ทั้งที่ `law-source.js` เขียนกำกับไว้แล้วว่าห้ามใส่ krisdika กลับ — พลาดเพราะเป็นคนละลิสต์

ผู้ใช้วางลิงก์ตามที่ UI โฆษณา → `fetch` โยน error → **500**
ถอดออกทั้ง `ALLOWED_HOSTS` และ `ALLOWED_SITES` ใน `src/pages/LawSummary.jsx`
(คอมเมนต์ในโค้ดบังคับให้สองลิสต์นี้ตรงกัน) · ตอนนี้ได้ 400 ที่อ่านรู้เรื่องแทน

---

## ไฟล์ที่แก้

| ไฟล์ | แก้อะไร |
|---|---|
| `api/_lib/osh-law-relate.js` | โควตา `specific` · `frontierWhy` · `anchorLeftOver` · ใช้ `WEB_SEARCH_TOOL` ตัวกลาง |
| `api/_lib/law-source.js` | ถอด `dlpw` · หมายเหตุ `doeb` · timeout 20 วิ · `web_search_20260209` · `SOURCE_URL_RULES` |
| `api/law-analyze.js` | ถอด `dlpw` + `krisdika` จาก `ALLOWED_HOSTS` |
| `src/pages/LawSummary.jsx` | ถอด `dlpw` + `krisdika` จาก `ALLOWED_SITES` |
| `SKILLS.md` | อัปเอกสารทั้ง 6 จุดให้ตรงกับโค้ด |

---

## ผลตรวจหลังแก้

```
✓ node --check ผ่านทั้ง 9 ไฟล์
✓ npm run build ผ่าน (105 modules)
✓ โหลดโมดูลจริง: type=web_search_20260209 · โดเมน 22 (เพดาน API 64)
✓ hostAllowed: ratchakitcha /documents/ ผ่าน · /DATA/PDF/ ตัด
                โดเมนนอกรายการตัด · dlpw.go.th ตัด
✓ จำลองโควตา 2,025 เคส: ไม่มีเคสที่ทำงานได้น้อยลง · ไม่มีเคสทะลุเพดาน
```

---

## ⚠️ ที่ต้องระวังต่อไป

**`web_search_20260209` ผูกกับรุ่นโมเดล** — ใช้ได้เฉพาะ Opus 5/4.8/4.7/4.6 · Sonnet 5 · Sonnet 4.6
`MODEL` ค่าเริ่มต้น `claude-sonnet-4-6` รองรับ แต่ถ้าตั้ง `ANTHROPIC_MODEL` ทับเป็นรุ่นเก่ากว่านั้น
**คำขอถูกตีกลับ 400 ทั้งคำขอ** ไม่ใช่แค่เครื่องมือไม่ทำงาน
→ เปลี่ยน `ANTHROPIC_MODEL` เมื่อไร ต้องเช็คบรรทัดนี้คู่กันเสมอ
→ deploy แล้วเจอ 400 ให้ดูที่ `ANTHROPIC_MODEL` ใน Vercel เป็นอันดับแรก

**ห้ามประกาศ `code_execution` คู่กับ `web_search_20260209`** — dynamic filtering รันโค้ดอยู่แล้วในตัว
(ตอนนี้โค้ดไม่ได้ประกาศอยู่แล้ว)

**ยังไม่ได้ยืนยัน:** `laws.anamai.moph.go.th/th/practices/download/?did=…&id=…`
ที่คอมเมนต์ลงวันที่ 2026-08-17 อ้างว่าเสิร์ฟ `application/pdf` 108 KB จริง
ทดสอบด้วย id ที่เดาเอาได้ 404 และ `/th/laws` ก็ 404 — **สรุปไม่ได้เพราะ id เป็นของปลอม**
ถ้ามี id จริงควรยิงซ้ำเพื่อยืนยันว่ากฎ `PDF_ENDPOINT_RE` ยังคุ้มค่าอยู่

**โฮสต์ที่ช้าผิดปกติ** (ไม่ได้แก้ เพราะยังตอบอยู่และด่าน content-type คุมได้)
`osh.labour.go.th` header 4.6 วิ · `www.pcd.go.th` connect timeout 10 วิบ้าง ตอบบ้าง
