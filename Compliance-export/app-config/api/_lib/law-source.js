// แหล่งตัวบทกฎหมาย · ส่วนที่ใช้ร่วมกันระหว่าง Skill 3 (osh-law-relate) กับโฟลว์คำถาม (anchor-answer)
//
// แยกออกมาเพราะทั้งสองฝั่งต้องใช้ด่านตรวจโดเมนและตัวดึงไฟล์ PDF ชุดเดียวกัน
// ถ้าปล่อยให้ import ข้ามกันจะเกิดวงกลม (osh-law-relate → anchor-answer → osh-law-relate)
// ซึ่ง ESM รันได้ก็จริงแต่ลำดับการ evaluate ขึ้นกับว่าใครถูกโหลดก่อน — เปราะเกินกว่าจะปล่อยไว้
//
// ที่สำคัญกว่านั้น: ด่านตรวจโดเมนต้องมีชุดเดียวในระบบ ก๊อปไปไว้สองที่แล้วมันจะค่อยๆ ต่างกัน
// แล้ววันหนึ่งฝั่งใดฝั่งหนึ่งจะรับโดเมนที่อีกฝั่งปฏิเสธ โดยไม่มีใครรู้ตัว

const MODEL = process.env.ANTHROPIC_MODEL || 'claude-sonnet-4-6'

// ── krisdika.go.th · ห้ามใส่กลับเข้ามา ───────────────────────────────────────
// เหตุผลเดิม: ค้นเจอแต่ไม่มีเนื้อหามาตราในดัชนี เพราะเก็บตัวบทเป็น PDF ผ่าน endpoint librarian/get
// ทดสอบซ้ำ 2026-08-16 เจอเหตุผลที่หนักกว่านั้น — ดึงไฟล์ไม่ได้เลยแม้จะรู้ URL:
//   krisdika.go.th          → DNS ไม่ resolve (ไม่มี A record ที่ apex)
//   www.krisdika.go.th      → SSL self-signed certificate · fetch() ปฏิเสธการเชื่อมต่อ
// ใส่เข้ามาก็ได้แค่ not_found ที่ช้าลง

// ── ชั้นที่ 1 · ต้นทางของกฎหมาย (หน่วยงานที่ออกกฎหมายนั้นเอง) ────────────────
export const PRIMARY_DOMAINS = [
  // ── SHE ──
  'ratchakitcha.soc.go.th',  // ต้นทางประกาศ path /documents/<id>.pdf สกัดข้อความได้
  'tosh.or.th',              // องค์การมหาชนตาม ม.52 มีตัวบท พ.ร.บ. เต็ม
  'labour.go.th',            // กรมสวัสดิการฯ และสำนักงานพื้นที่
  'dlpw.go.th',              // กรมสวัสดิการและคุ้มครองแรงงาน
  // ── กฎหมายด้านอื่นขององค์กร (หมวด LF) — ไม่มีกลุ่มนี้ Skill 3 จะคืน not_found เสมอ
  //    เพราะ source_url ที่ค้นเจอจะไม่ผ่านด่านตรวจโดเมนในข้อ (ก) ──
  'nbtc.go.th',              // กสทช. — โทรคมนาคม
  'mdes.go.th',              // กระทรวงดิจิทัลฯ — พ.ร.บ.คอมพิวเตอร์ และประกาศใต้กฎหมาย
  'pdpc.or.th',              // PDPA
  'ncsa.or.th',              // ความมั่นคงปลอดภัยไซเบอร์
  'ipthailand.go.th',        // ทรัพย์สินทางปัญญา
  'rd.go.th',                // สรรพากร
  'sso.go.th',               // ประกันสังคม
  'pcd.go.th',               // ควบคุมมลพิษ
  'onep.go.th',              // สผ. — สิ่งแวดล้อม
  'dbd.go.th',               // พัฒนาธุรกิจการค้า
  // ── P17 · กฎหมายอาคาร/ผังเมือง ──
  //    ตัวบทไทยอ้าง "ตามกฎหมายว่าด้วยการควบคุมอาคาร" บ่อยมาก (ห้องน้ำ ทางหนีไฟ แสงสว่าง ระบายอากาศ)
  //    ไม่มีโดเมนกลุ่มนี้ คำตอบจะตกด่านโดเมนทุกครั้งแล้วกลายเป็น "หาไม่เจอ" ทั้งที่ตัวบทเปิดได้
  //    ยืนยันแล้ว info.dpt.go.th เสิร์ฟไฟล์ตัวบทเป็น application/pdf จริง (ครอบคลุมด้วย dpt.go.th)
  'dpt.go.th',               // กรมโยธาธิการและผังเมือง — เจ้าของ พ.ร.บ.ควบคุมอาคาร + กฎกระทรวงใต้กฎหมาย
  'bangkok.go.th',           // กทม. — ข้อบัญญัติควบคุมอาคารในเขต กทม.

  // ── ปิดช่องโหว่: โดเมนที่ law-analyze.js เชื่อมาตลอด แต่ Skill 3 ไม่เคยเชื่อ ──
  //    ผลคือผู้ใช้วางลิงก์เองได้ แต่ระบบตามไปดึงเองไม่ได้ ทั้งที่เป็นแหล่งเดียวกัน
  //    เจอตอนทดสอบกฎกระทรวงควบคุมสถานประกอบกิจการฯ 2560 ซึ่งเป็นกฎกระทรวง "สาธารณสุข"
  //    แต่ moph.go.th ไม่อยู่ในรายการนี้ — กฎหมายแม่ของตัวบทที่กำลังสรุปเลยตามไปอ่านไม่ได้
  'moph.go.th',              // สธ. — ครอบคลุม ddc./anamai. (เจ้าของ พ.ร.บ.การสาธารณสุข)
  'diw.go.th',               // กรมโรงงานอุตสาหกรรม — "กฎหมายว่าด้วยโรงงาน" ที่ตัวบทอ้างถึงบ่อย
  'doeb.go.th',              // กรมธุรกิจพลังงาน — น้ำมันเชื้อเพลิง/ก๊าซ (หมวด LB)
  'disaster.go.th',          // ปภ. — การป้องกันและระงับอัคคีภัย (หมวด LC)
  'mnre.go.th',              // กระทรวงทรัพยากรธรรมชาติและสิ่งแวดล้อม
]

// ── ชั้นที่ 2 · ฉบับรวมที่จัดทำโดยองค์กรวิชาชีพ ──────────────────────────────
// ไม่ใช่ต้นฉบับราชกิจจาฯ แต่ "แม่นกว่า" สำหรับกฎหมายที่ถูกแก้หลายรอบ เพราะรวมฉบับแก้ไขไว้แล้ว
// พร้อมเชิงอรรถว่าข้อไหนถูกยกเลิกโดยฉบับใด
//
// ทำไมถึงคุ้ม — เคสจริงจากการทดสอบ:
//   ตารางที่ 2 ของกฎกระทรวง ฉบับที่ 39 (พ.ศ. 2537) ถูกยกเลิกโดยฉบับที่ 63 (พ.ศ. 2551)
//   ดึงราชกิจจาฯ ฉบับ 39 มาตรงๆ จะได้ "ตารางที่ถูกยกเลิกไปแล้ว" โดยไม่มีอะไรเตือนเลย
//   ซึ่งอันตรายกว่าการอ่านฉบับรวมที่มีเชิงอรรถบอกว่าตารางนี้ถูกแทนที่แล้ว
//
// แลกมาด้วยความเสี่ยงว่าฉบับรวมอาจอัปเดตไม่ทัน จึงติด note เตือนทุกครั้งที่คำตอบมาจากชั้นนี้
// ด่านตรวจอื่นยังบังคับครบเหมือนเดิม (source_excerpt · ตัวเลขต้องมีที่มา · ห้าม "ตามที่กฎหมายกำหนด")
export const SECONDARY_DOMAINS = [
  'asa.or.th',               // สมาคมสถาปนิกสยาม ในพระราชูปถัมภ์ — ฉบับรวมกฎหมายควบคุมอาคาร
  'shawpat.or.th',           // สมาคมส่งเสริมความปลอดภัยฯ — law-analyze เชื่ออยู่แล้ว
]

export const TRUSTED_DOMAINS = [...PRIMARY_DOMAINS, ...SECONDARY_DOMAINS]

// ── ที่อยู่ที่ "โดเมนถูก แต่เปิดไม่ได้จริง" ──────────────────────────────────
// ต่างจากโดเมนนอกรายการตรงที่ด่านโดเมนปล่อยผ่าน แล้วไปตายตอน fetch
// ราคาที่จ่าย: เสียคำขอ 1 ครั้งต่อ 1 คำถามเพื่อได้ค่าว่างกลับมา และผู้ใช้เห็นเหตุผลผิด
// ("ไม่อยู่ในโดเมนที่เชื่อถือได้" ทั้งที่โดเมนถูก — แค่ที่อยู่นั้นเลิกใช้ไปแล้ว)
//
// ยืนยัน 2026-08-17 ระหว่างสรุปกฎกระทรวงควบคุมสถานประกอบกิจการฯ 2560:
// คำถาม 8 ข้อ เสียคำขอไปกับ 2 ที่อยู่นี้ 4 ครั้ง โดยไม่ได้อะไรกลับมาสักครั้ง
const DEAD_SOURCES = [
  // web_search ยังคืนที่อยู่รูปแบบเก่านี้อยู่เรื่อยๆ เพราะดัชนีเก่ายังไม่หมดไป
  // ทดสอบ: /DATA/PDF/2556/A/113/9.PDF → ได้หน้า Cloudflare challenge (HTML 5 KB) ไม่ใช่ไฟล์
  { re: /^https?:\/\/(?:www\.)?ratchakitcha\.soc\.go\.th\/DATA\/PDF\//i,
    why: 'ที่อยู่เดิมของราชกิจจานุเบกษาถูกกันด้วย Cloudflare แล้ว — ตัวบทฉบับเดียวกันเปิดได้ที่ /documents/<id>.pdf' },
  // อาการเดียวกับ krisdika: ใบรับรองยืนยันไม่ผ่าน → fetch() ปฏิเสธก่อนถึงเนื้อหา
  { re: /^https?:\/\/legal\.labour\.go\.th(?:[:/]|$)/i,
    why: 'ใบรับรอง SSL ของโฮสต์นี้ยืนยันไม่ผ่าน fetch() จึงต่อไม่ติด (อาการเดียวกับ krisdika)' },
]

// คืน "เหตุผล" เมื่อที่อยู่นี้เปิดไม่ได้ · คืนค่าว่างเมื่อใช้ได้ตามปกติ
// มีเหตุผลติดมาด้วยเพราะ UI ต้องบอกให้ต่างจาก "โดเมนไม่น่าเชื่อถือ" ซึ่งคนละเรื่องกัน
export function deadSource(u){
  const s = String(u || '')
  return DEAD_SOURCES.find(d => d.re.test(s))?.why || ''
}

function inList(u, list){
  try{
    const host = new URL(u).hostname.toLowerCase()
    return list.some(d => host === d || host.endsWith('.' + d))
  }catch{ return false }
}

// ที่อยู่ที่รู้แล้วว่าเปิดไม่ได้ ถือว่าไม่ผ่านตั้งแต่ด่านนี้ — ประหยัดคำขอโดยไม่ผ่อนด่านตรวจใดเลย
// (ด่านหลักฐานยังบังคับเหมือนเดิมทุกข้อ ที่ตัดออกคือ "การไปดึงของที่ดึงไม่ได้อยู่แล้ว")
export function hostAllowed(u){ return !deadSource(u) && inList(u, TRUSTED_DOMAINS) }

// คำตอบมาจากฉบับรวมของเอกชน ไม่ใช่ต้นฉบับราชกิจจาฯ — ผู้ใช้ควรรู้ก่อนเอาไปอ้างอิงกับผู้ตรวจ
export function isSecondarySource(u){ return inList(u, SECONDARY_DOMAINS) }

// ── ดึงไฟล์ตัวบทจริงมาอ่าน แทนการเชื่อ snippet จาก web_search ────────────────
// ทดสอบจริง 2026-08-14: ราชกิจจาฯ ยอมให้ดึงเฉพาะไฟล์ .pdf ตรงๆ เท่านั้น
//   https://ratchakitcha.soc.go.th/documents/104277.pdf → 200 (ด้วย UA เดิมของเรา)
//   https://ratchakitcha.soc.go.th/documents/104277     → 403  (หน้า HTML)
//   https://www.ratchakitcha.soc.go.th/DATA/PDF/…       → 403  (path เว็บเวอร์ชันเก่า เลิกใช้แล้ว)
//                                                        2026-08-17 กลายเป็นหน้า Cloudflare → ใส่ DEAD_SOURCES แล้ว
// web_search อ่านได้แค่หัวข้อกับ snippet แตะเนื้อใน PDF ไม่ได้ → ไม่มี source_excerpt
// → โดนด่านตรวจหลักฐานตัดทิ้งทั้งก้อน → ขึ้น "หาตัวบทไม่พบ" ทั้งที่ไฟล์ตัวบทเปิดได้
const PDF_MAX_BYTES = 8_000_000   // กันไฟล์ใหญ่เกินจนคำขอไป Anthropic ล้ม
const PDF_FETCH_TIMEOUT = 45_000

// ── ที่อยู่ที่ "ไม่ลงท้ายด้วย .pdf แต่เสิร์ฟไฟล์ PDF จริง" ────────────────────
// เว็บหน่วยงานไทยจำนวนมากเสิร์ฟตัวบทผ่าน endpoint ดาวน์โหลดที่มีแต่ query string
// ยืนยัน 2026-08-17: laws.anamai.moph.go.th/th/practices/download/?did=…&id=…
//   → 200 · application/pdf · 108 KB (ตัวบทจริง)
// กฎ ".pdf เท่านั้น" ของเดิมจึงปิดตัวเองไม่ให้อ่านตัวบทของกรมอนามัยทั้งกรม
// ทั้งที่ไฟล์เปิดได้ — เป็นสาเหตุที่คำถามเรื่องเกณฑ์คุณภาพน้ำบริโภคตอบไม่ได้
//
// ด่านจริงยังเป็น content-type ข้างล่างเหมือนเดิม — ตรงนี้แค่เลิกตัดทิ้งตั้งแต่ยังไม่ได้ลอง
// ไม่ปล่อยให้ยิงมั่วทุก URL เพราะหน้า HTML ที่ยิงไปแล้วโดน content-type ตีกลับ ก็เสียเวลาฟรีอยู่ดี
const PDF_ENDPOINT_RE = /\/downloads?\/?(?:\?|$)|[?&](?:did|fid|file_id|attach(?:ment)?_id)=/i

export async function fetchPdfBase64(url){
  if(!hostAllowed(url)) return null                      // กัน SSRF — ต้องอยู่ในโดเมนที่เชื่อถือได้
  // ไฟล์ .pdf ตรงๆ หรือ endpoint ดาวน์โหลดที่รู้จัก · หน้า HTML ทั่วไปยังถูกตัดตั้งแต่ตรงนี้
  if(!/\.pdf($|\?|#)/i.test(url) && !PDF_ENDPOINT_RE.test(url)) return null
  try{
    const r = await fetch(url, {
      headers: { 'user-agent': 'Mozilla/5.0 LexRegistry' },
      signal: AbortSignal.timeout(PDF_FETCH_TIMEOUT),
    })
    if(!r.ok) return null
    if(!(r.headers.get('content-type') || '').toLowerCase().includes('pdf')) return null
    // เช็คขนาดจากหัว response ก่อนอ่าน body — ไม่งั้นดาวน์โหลดจบแล้วค่อยรู้ว่าใหญ่เกิน
    // เจอจริง: info.dpt.go.th/.../2522update.pdf (พ.ร.บ.ควบคุมอาคาร ฉบับรวม) = 165 MB
    // ของเดิมจะดูดครบ 165 MB เข้า memory ของ serverless function แล้วค่อยทิ้ง
    // ซึ่งกินทั้งเวลาในงบ 255 วิ และเสี่ยงทำให้ฟังก์ชันตายจาก memory limit
    const declared = Number(r.headers.get('content-length') || 0)
    if(declared > PDF_MAX_BYTES){ try{ await r.body?.cancel?.() }catch{} ; return null }
    const buf = await r.arrayBuffer()
    // ยังต้องเช็คซ้ำหลังอ่าน — เซิร์ฟเวอร์ที่ส่งแบบ chunked ไม่มี content-length มาให้
    if(!buf.byteLength || buf.byteLength > PDF_MAX_BYTES) return null
    return Buffer.from(buf).toString('base64')
  }catch{ return null }   // ดึงไม่ได้ไม่ใช่เหตุให้ล้ม — ถอยไปใช้ผลจาก web_search ตามเดิม
}

// ── ตัดเฉพาะหน้าที่มีตาราง แทนการส่งไฟล์ทั้งฉบับให้อ่าน ──────────────────────
//
// รอบถอดตารางเดิมส่ง PDF ทั้งฉบับ · กฎกระทรวงควบคุมอาคาร ฉบับที่ 55 มี 21 หน้า
// คิดเป็น ~45,000 token ต่อการอ่าน 1 ครั้ง ทั้งที่ตารางอยู่หน้าเดียว
// เรารู้อยู่แล้วว่าตารางอยู่หน้าไหน เพราะ pdf-parse แตกข้อความมาให้แล้ว
//
// ส่ง "ข้อความของหน้านั้น ±1 หน้า" แทนไฟล์ = เร็วขึ้นและถูกลงมาก โดยไม่ผ่อนด่านใด
// (source_excerpt ยังต้องคัดคำต่อคำจากข้อความที่ส่งไปเหมือนเดิม)
// เผื่อหน้าข้างเคียงเพราะตารางไทยข้ามหน้าบ่อย — ตารางที่ 2 ของฉบับที่ 39 กินหน้า 5–8
//
// คืน '' เมื่อทำไม่ได้ (หาหน้าไม่เจอ · ข้อความคุณภาพไม่พอ) ผู้เรียกถอยไปส่งไฟล์ทั้งฉบับตามเดิม
//
// เลือกหน้าที่พบ "ครั้งสุดท้าย" ไม่ใช่ครั้งแรก — ชื่อตารางโผล่ 2 ที่เสมอ คือในเนื้อความที่
// อ้างถึงมัน ("ให้เป็นไปตามตารางที่ 2 ท้ายกฎกระทรวงนี้") กับที่ตัวตารางเอง ซึ่งอยู่ท้ายเล่ม
// จับครั้งแรกจะได้หน้าเนื้อความที่ไม่มีตัวเลขสักตัว · แล้วไล่ไปข้างหน้า เพราะแถวของตารางไหลต่อ
// เทียบข้อความแบบทนความเพี้ยน — ตัดช่องว่าง แปลงเลขไทย และ **ตัดวรรณยุกต์/สระบน-ล่างทิ้ง**
// ข้อสุดท้ายจำเป็น เพราะ PDF ฉบับรวมของ ASA ทำวรรณยุกต์หายเป็นบางตัว
// "ตารางที่ 2" ในคำใบ้ กับ "ตารางที  2" ในไฟล์ ต้องเทียบกันติด ไม่งั้นหาหน้าไม่เจอเลย
const THAI_D = '๐๑๒๓๔๕๖๗๘๙'
const norm = s => String(s || '')
  .replace(/[๐-๙]/g, d => String(THAI_D.indexOf(d)))
  .replace(/[ัิ-ฺ็-๎]/g, '')   // วรรณยุกต์ · สระบน · สระล่าง
  .replace(/[\s​]/g, '')

export async function pdfPagesAround(b64, hint, after = 3, before = 1){
  let parser
  try{
    // "ตารางที่ 2 ท้ายกฎกระทรวง ฉบับที่ 39" → หาเลขตารางที่ต้องการก่อน
    //
    // ระยะคั่นต้อง 10 ไม่ใช่ 4 — ตัวบทเขียน "บัญชีหมายเลข 1" ซึ่งหลัง norm เหลือ
    // "บญชหมายเลข1" มีอักษรคั่น 7 ตัว เกินเพดานเดิม 4 จึงจับไม่ได้ คืนค่าว่าง
    // แล้วถอยไปส่ง PDF ทั้งฉบับแทน (ประกาศกรมอนามัยฯ น้ำบริโภค เจอเคสนี้)
    // ซึ่งย้อนกลับไปเสียทั้งเวลาและ token ที่การตัดหน้าตั้งใจประหยัด
    const GAP = '\\D{0,10}'
    const m = norm(hint).match(new RegExp(`(ตาราง|บญช|ภาคผนวก|แนบทาย)${GAP}(\\d+)`))
    if(!m) return ''
    // ในหน้าอาจเขียน "ตารางท 2" หรือ "ตารางท2" — ยอมให้มีอักขระคั่นได้เล็กน้อย
    const needle = new RegExp(m[1] + GAP + m[2] + '(?!\\d)')

    const { PDFParse } = await import('pdf-parse')
    parser = new PDFParse({ data: Buffer.from(b64, 'base64'), verbosity: 0 })
    const text = String((await parser.getText())?.text || '')
    // pdf-parse คั่นหน้าด้วย "-- N of M --" · ใช้เป็นเส้นแบ่งหน้า
    const pages = text.split(/\n--\s*\d+\s+of\s+\d+\s*--\n/)
    if(pages.length < 2) return ''

    let hit = -1
    pages.forEach((p, i) => { if(needle.test(norm(p))) hit = i })   // เอาครั้งสุดท้าย
    if(hit < 0) return ''

    const slice = pages.slice(Math.max(0, hit - before), hit + after + 1).join('\n')
    if(!slice.trim()) return ''

    // ด่านคุณภาพ — กันข้อความที่ถอดรหัสไม่ออก ไม่ใช่กันข้อความที่มีภาษาอังกฤษเยอะ
    //
    // เดิมใช้สัดส่วนอักขระไทย ≥ 50% ซึ่งเป็นเกณฑ์ของ "หน้าเนื้อความ" ไม่ใช่ของตาราง
    // ตารางเกณฑ์มักมีชื่อพารามิเตอร์และวิธีวิเคราะห์เป็นอังกฤษเต็มคอลัมน์
    // (บัญชีหมายเลข 1 ของประกาศกรมอนามัยฯ น้ำบริโภค: Nephelometry · ion chromatography ·
    //  AAS (graphite furnace) · Spectrophotometric) จนไทยเหลือต่ำกว่าครึ่ง แล้วถูกตีตกทั้งที่อ่านออกดี
    //
    // เกณฑ์ใหม่ดูจำนวนอักขระไทยเป็นจำนวนจริง ไม่ใช่สัดส่วน — หน้าที่ถอดรหัสไม่ออก
    // จะได้อักขระไทยใกล้ศูนย์อยู่แล้ว ซึ่งยังตกด่านนี้เหมือนเดิม
    const thai = (slice.match(/[฀-๿]/g) || []).length
    if(thai < 200 && thai / slice.length < 0.5) return ''
    return slice
  }catch{ return '' }
  finally{ try{ await parser?.destroy?.() }catch{} }
}

// โมเดลบางครั้งห่อด้วย fence หรือมีข้อความนำ — ลอกทีละชั้นจนกว่าจะ parse ได้
export function parseLoose(txt){
  let s = String(txt || '').trim().replace(/^```(?:json)?\s*/i, '').replace(/```\s*$/, '').trim()
  try{ return JSON.parse(s) }catch{}
  const m = s.match(/\{[\s\S]*\}/)
  if(m){ try{ return JSON.parse(m[0]) }catch{} }
  return null
}

// ── เรียก Claude แล้วคืน JSON ที่ parse ได้ · ห้าม throw ออกข้างนอก ───────────
// ทุกทางล้มเหลว (API ไม่ตอบ / JSON เพี้ยน / เน็ตหลุด) คืน null เหมือนกันหมด
// เพราะฝั่งเรียกจัดการเหมือนกันทั้งหมดอยู่แล้ว: ถอยไปใช้ผลที่ได้มาแล้ว
//
// ห้ามลดโมเดลของ call ที่แตะตัวบทกฎหมาย — คงเป็น MODEL (Sonnet) ทุกจุด
export async function askClaude({ system, content, tools = null, maxTokens = 8000, pdfBase64 = '' }){
  if(!process.env.ANTHROPIC_API_KEY) return null
  const headers = {
    'content-type': 'application/json',
    'x-api-key': process.env.ANTHROPIC_API_KEY,
    'anthropic-version': '2023-06-01',
  }
  if(pdfBase64) headers['anthropic-beta'] = 'pdfs-2024-09-25'

  const userContent = pdfBase64
    ? [{ type: 'document', source: { type: 'base64', media_type: 'application/pdf', data: pdfBase64 } },
       { type: 'text', text: content }]
    : content

  const body = { model: MODEL, max_tokens: maxTokens, system, messages: [{ role: 'user', content: userContent }] }
  if(tools) body.tools = tools

  try{
    const r = await fetch('https://api.anthropic.com/v1/messages', { method: 'POST', headers, body: JSON.stringify(body) })
    if(!r.ok) return null
    const data = await r.json()
    // เอาเฉพาะ block ที่เป็นข้อความ — ข้าม block ของ web_search (server_tool_use / web_search_tool_result)
    const txt = (data.content || []).filter(b => b.type === 'text').map(b => b.text).join('').trim()
    return parseLoose(txt)
  }catch{ return null }
}

// ── กฎเรื่อง "ที่อยู่ของไฟล์ตัวบท" ที่ต้องบอกโมเดลทุกครั้งที่ให้ค้นเว็บ ───────
// อยู่ไฟล์เดียวกับ DEAD_SOURCES ตั้งใจ — prompt กับด่านตรวจต้องเปลี่ยนพร้อมกันเสมอ
// แยกไปไว้คนละที่แล้วมันจะเพี้ยนจากกัน แล้วโมเดลจะคืนที่อยู่ที่โค้ดตัดทิ้งอยู่เรื่อยๆ
//
// ทำไมต้องบอกตั้งแต่รอบแรก แทนที่จะปล่อยให้ผิดแล้วค่อยยิงซ้ำ: การยิงซ้ำคือคำขอเพิ่มอีก 1 ครั้ง
// แต่ประโยคพวกนี้ฟรี · เจอจริงตอนสรุปกฎกระทรวงควบคุมสถานประกอบกิจการฯ 2560
// ผลค้นคืนที่อยู่รูปแบบเก่าของราชกิจจาฯ มา แล้วตกด่านโดเมนทั้งที่ตัวบทฉบับนั้นเปิดได้
export const SOURCE_URL_RULES = `เรื่องที่อยู่ (URL) ของไฟล์ตัวบท — คืนเฉพาะที่อยู่ที่เปิดได้จริง:
- ราชกิจจานุเบกษาเปิดได้เฉพาะรูปแบบ https://ratchakitcha.soc.go.th/documents/<เลข>.pdf
- รูปแบบเก่า https://www.ratchakitcha.soc.go.th/DATA/PDF/... ใช้ไม่ได้แล้ว (เว็บกันด้วย Cloudflare)
  ผลค้นยังคืนรูปแบบนี้มาบ่อยเพราะดัชนีเก่ายังไม่หมด — เจอแล้วห้ามใช้เป็น source_url
  ให้ค้นต่อจนได้ที่อยู่แบบ /documents/<เลข>.pdf หรือหาแหล่งอื่นในรายการโดเมนที่กำหนดแทน
- legal.labour.go.th เปิดไม่ได้ (ใบรับรอง SSL ไม่ผ่าน) ให้ใช้ osh.labour.go.th หรือ dlpw.go.th แทน
- ที่อยู่ที่ไม่ลงท้ายด้วย .pdf ใช้ได้ ถ้าเป็นลิงก์ "ดาวน์โหลดไฟล์ตัวบท" โดยตรง
  เช่น https://laws.anamai.moph.go.th/th/practices/download/?did=<เลข>&id=<เลข>
  แต่ห้ามคืนที่อยู่ของ "หน้าเว็บที่มีลิงก์ดาวน์โหลด" มาแทนตัวไฟล์`

export const WEB_SEARCH_TOOL = {
  type: 'web_search_20250305', name: 'web_search', max_uses: 5, allowed_domains: TRUSTED_DOMAINS,
}
